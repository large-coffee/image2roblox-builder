{
  "name": "@image2roblox/agent-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/agent-core/src/index.js",
  "types": "dist/agent-core/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@image2roblox/schemas": "workspace:*",
    "@image2roblox/shared": "workspace:*",
    "zod": "^4.1.4"
  }
}

{
  "name": "@image2roblox/roblox-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/roblox-generator/src/index.js",
  "types": "dist/roblox-generator/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@image2roblox/schemas": "workspace:*",
    "@image2roblox/shared": "workspace:*"
  }
}
